// import React from 'react';
// import Card from './Card';

// // random number generator
// function getRandomInt(max) {
// 	return Math.floor(Math.random() * max);
// }

// // random number saved to var num
// var num = getRandomInt(9999);

// // using random number for unique id for map b/c person.id won;t work for some reason

// function SearchList({ filteredPersons }) {
// 	const filtered = filteredPersons.map((person, num) => (
// 		<Card key={num} person={person} />
// 	));
// 	return <div>{filtered}</div>;
// }

// export default SearchList;
